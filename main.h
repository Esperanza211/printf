#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>
int main(void);
int _printf(const char *format, ...);
int _putchar(char c);
int handle_string(char *str);
#endif

